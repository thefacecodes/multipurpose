import React, { useContext } from "react";
// import { shopContext } from "./ShopContext";

// const defaultState = {
//     basket: [],
//     totalAmount: 0,
//   };

//   const reducer = (state, action) => {
//     if (action.type === "ADD") {
//       console.log(state);
//       const newBasket = [...state.basket, action.payload];

//       return {
//         ...state,
//         basket: newBasket,
//       };
//     }
//   };
// const reducer = (state, action) => {
//   if (action.type === "ADD") {
//     const newBasket = [...state, action.payload];
//     return {
//       ...state,
//       Basket: newBasket,
//     };
//   }
// };

// const [state, dispatch] = useReducer(defa, second, third)

// function ShopProvider({ defaultState, reducer, children }) {
//   const { defaultState, reducer } = useContext(shopContext);
//   //   const ShopSupplier = useContext(shopContext);

//   return <ShopSupplier.Provider>{children}</ShopSupplier.Provider>;
// }

// export default ShopProvider;
